---
title: 推断
description: ""
---

# 推断

<Player title="prompt_eng_05_inferring_v3" />

下一个视频是关于推理的。

我喜欢考虑这样的任务，即模型将文本作为输入并执行某种分析。

这可以是提取标签，提取名字，理解文本的情感，诸如此类的事情。

因此，如果你想从一段文本中提取一种情绪，无论是积极的还是消极的，在传统的机器学习工作流程中，你必须收集标签数据集，训练模型，弄清楚如何将模型部署在云中的某个地方并进行推断。

这可以很好地工作，但是，你知道，要完成这个过程需要做很多工作。

对于每一项任务，比如情感、提取名字和其他东西，你必须训练和部署一个单独的模型。

关于大型语言模型的一个非常好的事情是，对于像这样的许多任务，你可以只写一个提示，让它立即开始生成结果。

这给了应用程序开发极大的速度。

你也可以只使用一个模型，一个 API 来完成许多不同的任务，而不需要弄清楚如何训练和部署许多不同的模型。

有了这些，让我们进入代码看看如何利用它。

这是我们常用的起始代码。

我来运行一下。

我要用的最合适的例子是对一盏灯的回顾。

所以，`卧室里需要一盏漂亮的灯，而这盏灯有额外的储物空间`等等。

所以，让我写一个提示来分类这种情绪。

如果我想让系统告诉我，你知道，情绪是什么。

我可以只写，`下面的产品评论的观点是什么?`加上通常的分隔符和评论文本等等，让我们运行它。

上面写着"产品评论的情绪是正面的。

，这似乎是正确的。

这盏灯并不完美，但这位顾客似乎很满意。

看起来是一家关心客户和产品的伟大公司。

我认为积极的情绪似乎是正确的答案。

现在它打印出整个句子，“产品评论的情绪是积极的。

“如果你想给出一个更简洁的回答，以便于后期处理，我可以根据这个提示，再加一条指令，给你一个单词的答案，可以是肯定的，也可以是否定的。

所以它只是像这样打印出正数，这使得一段文本更容易接受这个输出并处理它并对它做一些事情。

让我们看看另一个提示符，仍然使用灯复习。

在这里，我有，“找出下面评论的作者所表达的情绪列表。

在这个列表中不要包含超过 5 个项目。

“所以，大型语言模型非常擅长从一段文本中提取特定的东西。

在这种情况下，我们表达的是情感，这可能有助于理解你的客户对特定产品的看法。

对于许多客户支持组织来说，了解某个特定用户是否非常沮丧是很重要的。

所以，你可能有一个不同的分类问题，像这样，`下面评论的作者是否表达了愤怒?`

因为如果有人真的很生气，可能需要额外的关注来进行客户评论，获得客户支持或客户成功，了解发生了什么，并为客户做正确的事情。

在这种情况下，客户并没有生气。

注意到有监督学习，如果我想建立所有这些分类器，我不可能在这几分钟的时间里完成这个你在这个视频中看到的有监督学习。

我鼓励你暂停这个视频，并尝试更改其中的一些提示。

也许可以问问客户是否表示满意，或者询问是否有缺失的部件，看看你是否可以提示对这个灯的评论做出不同的推断。

让我来展示更多你可以用这个系统做的事情，特别是从客户评论中提取更丰富的信息。

所以，信息提取是 NLP 的一部分，自然语言处理的一部分，它涉及到从一段文本中提取出你想要知道的某些东西。

因此，在这个提示中，我要求它识别以下物品、购买的物品和制造该物品的公司名称。

同样，如果你试图从一个在线购物电子商务网站上总结许多评论，它可能会对你的大量评论收集有用，以找出是什么物品，谁制造了这个物品，找出积极和消极的情绪，跟踪对特定物品或特定制造商的积极或消极情绪的趋势。

在本例中，我将要求它将您的响应格式化为一个 JSON 对象，其中`Item`和`Brand`作为键。

如果我这样做，它会说这个物品是一盏灯，品牌是 Lumina，你可以很容易地把它加载到 Python 字典中，然后对这个输出做额外的处理。

在我们所经历的例子中，你看到了如何编写一个提示来识别情绪，判断某人是否生气，然后提取物品和品牌。

提取所有这些信息的一种方法是使用三到四个提示并调用"get_completion"三到四次来提取这些不同的视图一次一个。

但实际上你可以写一个提示符来同时提取所有这些信息。

所以我们说`识别以下物品，提取情绪，是评论者表达愤怒，购买的物品，制造它的公司`。

然后这里我还会告诉它将愤怒值格式化为布尔值，让我运行一下。

这输出一个 JSON，其中情绪是积极的，愤怒，然后 false 周围没有引号，因为它要求它只输出一个布尔值。

提取项目为`灯与额外的存储空间`，而不是灯，似乎没有问题。

但是通过这种方式，您只需一个提示符就可以从一段文本中提取多个字段。

像往常一样，请随意暂停视频，自己尝试不同的变体。

或者甚至可以尝试键入完全不同的评论，看看你是否仍然可以准确地提取这些东西。

现在，我看到的大型语言模型的一个很酷的应用是推断主题。

给定一段很长的文字，你知道，这段文字是关于什么的?主题是什么?这是一篇虚构的报纸文章，关于政府工作人员对他们工作的机构的感受。

最近政府进行的调查，等等。

结果显示，NASA 是一个受欢迎的部门，满意度很高。

“我是 NASA 的粉丝，我喜欢他们的工作，但这是一篇虚构的文章。

因此，对于这样一篇文章，我们可以要求它，在这个提示下，确定以下文本中正在讨论的五个主题。

对于我的回答，让我们用逗号分隔列表，让每个条目都有一到两个单词长。

所以，如果我们运行它，你知道，我们得到这篇文章。

它是关于政府调查，关于工作满意度，关于 NASA，等等。

所以，总的来说，我认为这是一个很好的主题列表。

当然，你也可以，你知道，把它分开，这样你就可以得到一个包含这篇文章所涉及的五个主题的 Python 列表。

如果您有一个文章集合并提取主题，那么您还可以使用大型语言模型来帮助您索引到不同的主题。

所以，让我用一个稍微不同的主题列表。

假设我们是一个新闻网站，你知道，这些是我们追踪的话题。

"NASA，地方政府，工程部，员工满意度，联邦政府"

假设你想算出，给定一篇新闻文章，这篇文章涵盖了哪些主题。

这是我可以使用的提示符。

我要说的是，确定最终主题列表中的每个项目是否在下面的文本中是一个主题。

对每个题目给出 0 或 1 分的答案。

所以，很好。

这是和之前一样的故事文本。

所以，这是一个故事。

是关于 NASA 的。

这与地方政府无关。

这与工程无关。

这关系到员工满意度，关系到联邦政府。

因此，在机器学习中，这有时被称为`零射击学习算法`，因为我们没有给它任何标记的训练数据，所以这是零射击。

只需一个提示，它就能确定这篇新闻文章涵盖了哪些主题。

所以，如果你想要生成一个新闻警报，比如说，处理新闻，我真的很喜欢 NASA 所做的很多工作。

所以，如果你想建立一个系统，它可以获取这些信息，把这些信息放进字典里，每当 NASA 的新闻弹出时，打印`警惕:NASA的新故事!`，他们可以用这个来快速地获取任何文章，找出它是关于什么主题的，如果这个主题包括 NASA，打印出`警惕:NASA的新故事!`。

哦，只有一件事。

我用这个主题词典。

我在这里使用的提示符不是很健壮。

如果我想要一个生产系统，我可能会让它以 JSON 格式输出答案，而不是以列表的形式输出，因为大型语言模型的输出可能会有点不一致。

这实际上是一段很脆弱的代码。

但如果你想，当你看完这个视频，你可以随意看看你是否能弄清楚如何修改这个提示，让它输出 JSON 而不是像这样的列表，然后有一个更可靠的方法来判断一个特定的文章是否是关于 NASA 的故事。

这就是推断。

在短短几分钟内，你就可以构建多个系统来对文本进行推理，而以前一个熟练的机器学习开发人员只需要几天甚至几周的时间。

所以，我发现这非常令人兴奋，无论是对熟练的机器学习开发人员，还是对机器学习不熟悉的人，你现在可以使用提示来非常快速地构建并开始对像这样非常复杂的自然语言处理任务进行推断。

在下一个视频中，我们将继续讨论使用大型语言模型可以做的令人兴奋的事情，我们将继续`转换`。

如何将一段文本转换成另一段文本，比如翻译成另一种语言。

我们继续下个视频。
