---
title: 转换
description: ""
---

# 转换

<Player title="prompt_eng_06_transforming_v3" />

大型语言模型非常擅长将其输入转换为不同的格式，例如以一种语言输入一段文本并将其转换或翻译为另一种语言，或者帮助进行拼写和语法更正。

因此，将一段可能不完全符合语法的文本作为输入，并帮助您稍微修复它，甚至转换格式，例如输入 HTML 和输出 JSON。

我曾经用一堆正则表达式写过一堆应用程序，有些痛苦，现在用一个大的语言模型和一些提示来实现这些应用程序肯定会简单得多。

是的，这些天我几乎用 ChatGPT 来校对我写的所有东西，所以我很高兴现在向你们展示笔记本上更多的例子。

首先，我们将导入 OpenAI，并使用我们在整个视频中一直使用的 get_completion 辅助函数。

我们首先要做的是翻译任务。

所以大型语言模型是在许多来源的大量文本上训练的，其中很多是互联网，当然，这是在许多不同的语言中。

这就赋予了模型翻译的能力。

这些模型了解数百种语言，熟练程度各不相同。

我们将通过一些例子来说明如何使用这种能力。

让我们从简单的开始。

在第一个例子中，提示是将下面的英语文本翻译成西班牙语。

"你好，我想订购一台搅拌机"

回答是`你好，我gustaría ordenar una licuadora`。

我很抱歉你们所有说西班牙语的人。

不幸的是，我从来没有学过西班牙语，你肯定能看出来。

好的，让我们再看一个例子。

在这个例子中，提示符是`告诉我这是什么语言`。

然后这是法语，`Combien co<s:1> te le lampadaire`。

我们运行一下。

这个模型已经识别出“这是法国的。

该模型还可以同时进行多种翻译。

在这个例子中，让我们把下面的文本翻译成法语和西班牙语。

你知道吗?让我们再加上一个英国海盗。

文字是`我想订一个篮球`。

这里有法国海盗，西班牙海盗和英国海盗。

所以在一些语言中，翻译可能会根据说话者和听者的关系而变化。

你也可以把这个解释给语言模型。

所以它会相应地进行翻译。

所以在这个例子中，我们说，`把下面的文本翻译成西班牙语，包括正式形式和非正式形式`。

`你想订一个枕头吗?`注意这里我们用的分隔符不同于这些反号。

只要有明确的区分，这并不重要。

这里我们有正式的和非正式的。

所以正式是当你和一个比你年长的人说话或者你在一个专业的场合。

这是你使用正式语气的时候，而非正式是你和一群朋友说话的时候。

我不会说西班牙语，但我爸爸会，他说这是对的。

对于下一个示例，我们将假设我们负责一家跨国电子商务公司，因此用户消息将使用各种不同的语言，因此用户将使用各种语言告诉我们他们的 IT 问题。

所以我们需要一个通用翻译器。

首先我们会粘贴一个用户消息列表用各种不同的语言。

现在我们将循环遍历这些用户消息。

所以`for issue in user_messages`。

然后我要复制这个稍长的代码块。

所以我们要做的第一件事就是让模型告诉我们问题是用什么语言写的。

这是提示符。

然后我们将打印出原始消息的语言和问题。

然后我们会让模型把它翻译成英语和韩语。

我们运行一下。

这是法语原文。

所以我们有多种语言，然后模型将它们翻译成英语和韩语。

你可以在这里看到，模型说，`这是法国的`。

这是因为这个提示的响应将是`this is French`。

你可以试着编辑这个提示，告诉我这是什么语言，只用一个词回答，或者不要用句子，诸如此类的事情。

如果你想让这只是一个词。

或者你可以请求 JSON 格式之类的，这可能会鼓励它不要使用整个句子。

太神奇了，你造了一个通用翻译器。

你也可以暂停视频，加入其他你想学的语言。

也许你自己会说一些语言，然后看看模型是如何运作的。

接下来我们要深入研究的是音调转换。

写作可以根据目标受众而有所不同，你知道，我给同事或教授写电子邮件的方式显然会和我给弟弟发短信的方式大不相同。

因此，ChatGPT 实际上也可以帮助产生不同的音调。

那么，让我们来看一些例子。

因此，在第一个例子中，提示是`将下面的俚语翻译成商业信函`。

“伙计，我是乔，看看立灯的规格。

“那么，让我们执行这个。

正如你所看到的，我们有一封更正式的商务信，里面有一份关于立灯规格的建议。

接下来我们要做的是在不同的格式之间进行转换。

ChatGPT 非常擅长在不同格式之间进行转换，比如 JSON 到 HTML、XML 等等。

减价。

因此，在提示符中，我们将描述输入和输出格式。

这里有一个例子。

我们有这个 JSON，它包含一个餐厅员工的列表，以及他们的姓名和电子邮件。

然后在提示符中，我们将要求模型将其从 JSON 转换为 HTML。

因此，提示符是`将以下Python字典从JSON转换为具有列头和标题的HTML表`。

然后我们将从模型中得到响应并打印出来。

这里我们有一些显示所有员工姓名和电子邮件的 HTML。

现在我们看看能否查看这个 HTML。

我们将使用 Python 库中的显示函数"display (HTML(response))"

你可以看到这是一个格式正确的 HTML 表格。

我们要做的下一个转换任务是拼写检查和语法检查。

这是 ChatGPT 的一种非常流行的用法。

我强烈建议你这么做，我一直都这么做。

当你用非母语工作时，这尤其有用。

这里有一些常见语法和拼写问题的例子，以及语言模型如何帮助解决这些问题。

所以我要粘贴一些有语法或拼写错误的句子。

然后我们要循环遍历每一个句子，并要求模型对它们进行校对。

校对并改正。

然后我们会使用一些分隔符。

然后我们将得到响应并像往常一样打印它。

所以这个模型能够纠正所有这些语法错误。

我们可以使用之前讨论过的一些技巧。

所以我们可以，为了提高提示，我们可以说，校对和纠正以下文本。

和重写。

然后重写整个。

重写一下。

修正版本。

如果没有发现任何错误，就说没有发现任何错误。

让我们试试这个。

这样我们就可以，哦，这里还是用引号。

但你可以想象，你可以找到一种方法，用一点迭代的快速开发。

找到一个每次都更可靠的提示符。

现在我们再做一个例子。

在你发布到公共论坛之前，检查你的文本总是有用的。

我们来看一个检查评论的例子。

这是一篇关于熊猫毛绒玩具的评论。

所以我们会让模特来校对和更正评论。

太好了。

所以我们有了这个修正的版本。

我们能做的一件很酷的事情就是找出我们最初的评论和模型的输出之间的差异。

所以我们将使用这个红线 Python 包来完成这个。

我们将得到我们的评论的原始文本和模型输出之间的差异，然后显示这个。

所以在这里你可以看到原始审查和模型输出之间的差异以及已经被纠正的东西。

所以，我们使用的提示是，`校对并修改这篇评论`。

但你也可以做一些更戏剧性的改变，改变语气，诸如此类。

那么，让我们再试一件事。

因此，在这个提示中，我们将要求模型对同一篇评论进行校对和更正，但也要使其更引人注目，并确保它遵循 APA 风格，并针对高级读者。

我们还将要求以降价格式输出。

所以我们用的是和原始评论一样的文字。

我们来执行这个。

这里我们有一个扩展的 APA 风格的软熊猫评论。

这就是变换视频的内容。

下一个是`扩展`，我们将采用较短的提示，并从语言模型中生成较长，更自由的响应。
